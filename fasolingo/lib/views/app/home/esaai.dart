import 'package:flutter/material.dart';

class EsaaiPage extends StatelessWidget {
  const EsaaiPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}