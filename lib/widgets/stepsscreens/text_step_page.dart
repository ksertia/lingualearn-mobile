import 'package:flutter/material.dart';

class TextStepPage extends StatelessWidget {
  final dynamic data;

  const TextStepPage({super.key, required this.data});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("xfddfjkl")),
    );
  }
}