import 'package:flutter/material.dart';

class QuizStepPage extends StatelessWidget {
  final dynamic data;

  const QuizStepPage({super.key, required this.data});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("ttykjhbvxdxdx")),
    );
  }
}